apiVersion: v1
kind: ResourceQuota
metadata:
  name: pods-low
  labels:
    quota: "low"
spec:
  hard:
    cpu: 2
    memory: 8Gi
    pods: 4
    limits.memory: 12Gi
  scopeSelector:
    matchExpressions:
    - operator : In
      scopeName: PriorityClass
      values: ["low"]
