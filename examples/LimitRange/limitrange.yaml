apiVersion: v1
kind: LimitRange
metadata:
  name: test-mem-limit-range
  labels:
    containerSize: minimal
spec:
  limits:
  - default:
      memory: 512Mi
    defaultRequest:
      memory: 256Mi
    type: Container
