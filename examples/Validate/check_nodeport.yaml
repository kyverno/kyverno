apiVersion : kyverno.io/v1alpha1
kind: Policy
metadata:
  name: check-host-path
spec:
  rules:
  - name: check-host-path
    resource:
      kinds:
      - Service
    validate:
      message: "Only NodePort type is allowed"
      pattern:
        spec:
          type: "NodePort"
