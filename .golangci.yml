linters:
  enable:
    - gosec
    - errcheck
    - gosimple
    - bodyclose
    - staticcheck
  disable:
    - ineffassign
    - deadcode
    - unused
    - structcheck

run:
  timeout: 5m
  skip-files:
    - ".+_test.go"
    - ".+_test_.+.go"