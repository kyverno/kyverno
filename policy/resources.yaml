apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pass-ingress
  namespace: default
spec:
  rules:
  - host: "mydomain.io"
    http:
      paths:
      - backend:
          service:
            name: servicea
            port:
              number: 80
        path: /
        pathType: Prefix
  - host: "my.io"
    http:
      paths:
      - backend:
          service:
            name: servicea
            port:
              number: 80
        path: /
        pathType: Prefix