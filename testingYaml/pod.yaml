apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    name: hello-world
spec:
  containers:
    - name: hello-world
      image: hello-world
      ports:
        - containerPort: 81
#      resources:
#        limits:
#          memory: "30Mi"
#          cpu: "0.2"
#        requests:
#          memory: "20Mi"
#          cpu: "0.1"