apiVersion: example.com/v1
kind: Foo
metadata:
  name: demo-foo
  namespace: default
spec:
  containers:
    - name: app
      image: 'ghcr.io/kyverno/test-verify-image:signed'
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
        requests:
          memory: "64Mi"
          cpu: "250m"
