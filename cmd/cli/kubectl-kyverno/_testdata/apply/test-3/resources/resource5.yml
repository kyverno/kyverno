apiVersion: example.com/v1
kind: Foo
metadata:
  name: foo-sample
spec:
  containers:
    - name: app-container
      image: nginx:latest
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
        requests:
          cpu: "250m"
          memory: "128Mi"
