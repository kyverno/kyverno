# Allocation ceilings for the admission hot-path benchmark gate (#17509).
#
# Format: <benchmark-name> <max_allocs_per_op> <max_bytes_per_op>
#
# Values are measured at commit 2360fc7 with:
#   go test -run=^$ -bench=. -benchmem -benchtime=100x -count=1 \
#     ./pkg/engine/... ./pkg/policycache/... ./pkg/webhooks/resource/...
# (also validated for stability with -count=5/-count=10).
#
# Headroom: +10% on allocs/op, +25% on bytes/op, rounded up to a whole
# number, except BenchmarkHandlerValidate which uses +20% allocs/op headroom
# (see that benchmark's doc comment for why: pond-pool/informer goroutine
# noise makes its allocs/op vary by a few per run across otherwise-identical
# runs, unlike the other three benchmarks which are allocs/op-identical
# across -count=10).
#
# If your PR legitimately changes allocation counts at one of these
# boundaries, re-measure with `make test-perf` and update this file in the
# same PR, with a one-line justification in the PR description.

# name                                    max_allocs_per_op  max_bytes_per_op
BenchmarkEngineValidate                   302                36059
BenchmarkContextCheckpointRestore         18                 3475
BenchmarkGetPolicies/policies-1           16                 2302
BenchmarkGetPolicies/policies-10          16                 2302
BenchmarkGetPolicies/policies-100         16                 2302
BenchmarkGetPolicies/policies-1000        16                 2302
BenchmarkHandlerValidate                  1194               93204
