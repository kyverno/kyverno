initContainer:
  extraArgs:
    - --loggingFormat=text

extraArgs:
  - --loggingFormat=json
  - --enableTracing
  - --tracingAddress=kyverno-tempo.kyverno
  - --tracingPort=4317

cleanupController:
  rbac:
    clusterRole:
      extraResources:
        - apiGroups:
            - ''
          resources:
            - pods
