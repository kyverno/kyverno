initContainer:
  extraArgs:
    - --loggingFormat=text

extraArgs:
  - --loggingFormat=json
  - --enableTracing
  - --tracingAddress=tempo.monitoring
  - --tracingPort=4317

cleanupController:
  rbac:
    clusterRole:
      extraResources:
        - apiGroups:
            - ''
          resources:
            - pods
