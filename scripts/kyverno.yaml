initContainer:
  extraArgs:
    - --loggingFormat=text

extraArgs:
  - --loggingFormat=text
  - --enableTracing
  - --tracingAddress=tempo.monitoring
  - --tracingPort=4317

cleanupController:
  rbac:
    clusterRole:
      extraResources:
        - apiGroups:
            - ''
          resources:
            - pods
