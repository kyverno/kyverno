initContainer:
  extraArgs:
    - --loggingFormat=text

extraArgs:
  - --loggingFormat=json
  - --enableTracing
  - --tracingAddress=kyverno-tempo.kyverno
  - --tracingPort=4317

# grafana:
#   enabled: true

cleanupController:
  rbac:
    clusterRole:
      extraResources:
        - apiGroups:
            - ''
          resources:
            - pods
