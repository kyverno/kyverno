apiVersion: v1
kind: Pod
metadata:
  labels:
    run: test
  name: test
spec:
  containers:
  - image: public.ecr.aws/docker/library/nginx
    name: test
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
