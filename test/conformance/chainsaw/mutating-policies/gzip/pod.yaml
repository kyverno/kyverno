apiVersion: v1
kind: Pod
metadata:
  name: pod01
  annotations:
    coco.io/initdata-configmap: initdata-config
  labels:
    coco: enabled
spec:
  containers:
  - name: pod01-01
    image: nginx:latest
