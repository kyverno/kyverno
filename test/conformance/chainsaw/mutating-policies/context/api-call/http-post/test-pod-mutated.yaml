apiVersion: v1
kind: Pod
metadata:
  namespace: default
  name: test-pod
  labels:
    app: demo 
    postLabel: from-post
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:latest