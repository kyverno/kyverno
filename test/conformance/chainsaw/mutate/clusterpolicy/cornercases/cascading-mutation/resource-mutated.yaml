apiVersion: v1
kind: Pod
metadata:
  labels:
    backup-needed: "yes"
    type: database
  name: cassandra
  namespace: default
spec:
  containers:
  - image: public.ecr.aws/docker/library/cassandra:latest
    imagePullPolicy: Always
    name: cassandra