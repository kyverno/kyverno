apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: chip-special-role
  namespace: qa
rules:
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - create
  - list
  - watch
