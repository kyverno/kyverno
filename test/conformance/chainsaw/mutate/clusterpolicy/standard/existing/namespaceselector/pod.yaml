apiVersion: v1
kind: Pod
metadata:
  name: test-org
  namespace: org-label-inheritance-existing-standard-ns
spec:
  terminationGracePeriodSeconds: 10
  containers:
  - image: nginx:latest
    name: test-org
