# SigstoreBundle with static key verification

This test verifies that `type: SigstoreBundle` correctly verifies signatures
using a static public key. The signed image has Sigstore bundle referrers
stored via the OCI Referrers API.
