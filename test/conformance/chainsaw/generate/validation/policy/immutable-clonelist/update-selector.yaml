apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: generate-update-clonelist
  namespace: default
spec:
  rules:
  - name: sync-secret
    match:
      any:
      - resources:
          kinds:
          - 
    generate:
      generateExisting: false
      namespace: default
      synchronize : true
      cloneList:
        namespace: default
        kinds:
          - v1/Secret
          - v1/ConfigMap
        selector:
          matchLabels:
            foo: "bar"
