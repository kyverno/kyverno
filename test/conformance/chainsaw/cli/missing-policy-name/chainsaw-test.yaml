apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: test
spec:
  steps:
  - name: invalid policy name
    try:
    - script:
        content: kyverno test .
        check:
          ($error != null): true
          ($stderr): "Error: failed to run test (The policy named missing cannot be found.\nThe rule require-image-tag cannot be found in the policy named missing.\n)"
