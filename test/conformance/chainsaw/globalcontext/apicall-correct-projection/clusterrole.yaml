apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: apicall-projections-test-reader
rules:
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch"]
