# After SSA update (triggered by namespace label change), the drifted value must be restored.
apiVersion: v1
kind: ConfigMap
metadata:
  name: ssa-update-config
  namespace: ssa-update-ns
data:
  mode: ssa
  key: original
