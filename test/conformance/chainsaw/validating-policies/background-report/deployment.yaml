apiVersion: apps/v1
kind: Deployment
metadata:
  name: background-report
  labels:
    app: test
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - name: test
        image: 'ghcr.io/kyverno/test-verify-image:unsigned'
