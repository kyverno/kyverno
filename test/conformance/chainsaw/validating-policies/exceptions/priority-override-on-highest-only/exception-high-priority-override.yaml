apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: high-priority-audit-override
  labels:
    polex.kyverno.io/priority: "10"
spec:
  policyRefs:
    - name: require-team-label
      kind: ValidatingPolicy
      validationAction: Audit
  matchConditions:
    - name: match-pod
      expression: "object.metadata.name == 'test-pod'"
