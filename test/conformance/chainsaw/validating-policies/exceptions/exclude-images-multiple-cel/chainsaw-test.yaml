apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: exclude-images-multiple-cel
spec:
  steps:
  - name: create exception
    try:
    - create:
        file: exception.yaml
    - sleep:
        duration: 10s
  - name: create policy
    use:
      template: ../../../_step-templates/create-policy.yaml
      with:
        bindings:
        - name: file
          value: policy.yaml
  - name: wait-validating-policy-ready
    use:
      template: ../../../_step-templates/validating-policy-ready.yaml
      with:
        bindings:
        - name: name
          value: exclude-images-multiple-cel
  - name: create bad-pod.yaml
    try:
    - apply:
        expect:
        - check:
            ($error != null): true
        file: bad-pod.yaml
  - name: create skipped-pod
    try:
    - apply:
        file: skipped-pod.yaml
