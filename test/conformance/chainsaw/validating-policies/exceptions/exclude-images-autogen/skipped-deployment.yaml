apiVersion: apps/v1
kind: Deployment
metadata:
  name: skipped-deployment
  labels:
    prod: "true"
    app: good-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      prod: "true"
      app: good-nginx
  template:
    metadata:
      labels:
        prod: "true"
        app: good-nginx
    spec:
      containers:
      - name: nginx
        image: public.ecr.aws/docker/library/nginx:latest
        securityContext:
          seccompProfile:
            type: Unconfined
