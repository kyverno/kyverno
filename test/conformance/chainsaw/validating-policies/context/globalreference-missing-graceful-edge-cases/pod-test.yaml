apiVersion: v1
kind: Pod
metadata:
  name: test-pod-multiple-missing
  namespace: test-globalcontext-edge-cases
  labels:
    test: multiple-missing-refs
spec:
  containers:
  - name: test
    image: registry.k8s.io/pause:latest
    resources:
      requests:
        cpu: 10m
        memory: 10Mi
      limits:
        cpu: 10m
        memory: 10Mi
  terminationGracePeriodSeconds: 0 