apiVersion: kyverno.io/v2beta1
kind: CleanupPolicy
metadata:
  name: deprecations-cleanup-v2beta1
  namespace: ($namespace)
spec:
  schedule: "0 0 * * *"
  match:
    any:
    - resources:
        kinds:
        - Pod
        names:
        - deprecations-does-not-exist
