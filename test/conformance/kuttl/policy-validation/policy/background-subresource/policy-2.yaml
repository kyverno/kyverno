apiVersion: kyverno.io/v1
kind: Policy
metadata:
  name: deny
spec:
  validationFailureAction: Audit
  background: true
  rules:
    - name: deny
      match:
        any:
          - resources:
              kinds:
                - Pod/scale
      validate:
        deny: {}
