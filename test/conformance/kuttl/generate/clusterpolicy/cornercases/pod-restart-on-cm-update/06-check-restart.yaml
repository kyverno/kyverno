apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl get po -n kube-state-metrics
  - script: "if [ \"$(kubectl get po -n kube-state-metrics | awk 'NR==2{print $1}')\" != \"$(cat pod-name.txt)\" ];then exit;else (exit 1);fi"