apiVersion: kyverno.io/v2beta1
kind: ClusterPolicy
metadata:
  name: cpol-foreach-data-nosync
spec:
  rules:
    - name: cpol-foreach-data-nosync-secret
      match:
        any:
          - resources:
              kinds:
                - Pod
      generate:
        foreach:
          - list: "request.object.spec.containers"
            generateResources:
              - kind: Secret
                apiVersion: v1
                name: "newsecret"
                namespace: "default"
                synchronize: false
                data:
                  foo: YmFy
