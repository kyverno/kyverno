apiVersion: v1
data:
  foo: YmFy
kind: Secret
metadata:
  name: regcred
  namespace: default
type: Opaque
---
apiVersion: kyverno.io/v2beta1
kind: Policy
metadata:
  name: pol-foreach-clone-nosync
  namespace: default
spec:
  rules:
  - name: pol-foreach-clone-nosync-secret
    match:
      any:
      - resources:
          kinds:
          - pod
    generate:
      foreach:
      - list: "request.object.spec.containers"
        generateResources:
        - kind: Secret
          apiVersion: v1
          name: "newsecret"
          namespace: "default"
          synchronize: false
          clone:
            namespace: default
            name: regcred
