apiVersion: kyverno.io/v2beta1
kind: Policy
metadata:
  name: pol-foreach-data-sync
  namespace: default
spec:
  rules:
    - name: pol-foreach-data-sync-secret
      match:
        any:
          - resources:
              kinds:
                - Pod
      generate:
        foreach:
          - list: "request.object.spec.containers"
            generateResources:
              - kind: Secret
                apiVersion: v1
                name: "newsecret"
                namespace: "default"
                synchronize: true
                data:
                  foo: YmFy
