# file path relative to project root
input:
  policy: test/scenarios/policy_validate_imageRegistries.yaml
  resource: test/scenarios/resources/resource_validate_imageRegistries.yaml
expected:
  validation:
    policyresponse:
      policy: check-registries
      resource:
        kind: Deployment
        apiVersion: apps/v1
        namespace: ''
        name: nirmata-nginx
      rules:
        - name: check-registries
          type: Validation
          message: Validation rule 'check-registries' succesfully validated
          success: true