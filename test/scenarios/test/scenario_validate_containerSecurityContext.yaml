# file path relative to project root
input:
  policy: test/scenarios/validate/policy_validate_containerSecurityContext.yaml
  resource: test/scenarios/resources/resource_validate_containerSecurityContext.yaml
expected:
  validation:
    policyresponse:
      policy: container-security-context
      resource:
        kind: Deployment
        apiVersion: apps/v1
        namespace: ''
        name: csc-demo-unprivileged
      rules:
        - name: validate-user-privilege
          type: Validation
          message: Validation rule 'validate-user-privilege' succesfully validated
          success: true     