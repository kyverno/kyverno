apiVersion: apps/v1
kind: Deployment
metadata:
  name: fail-deploy
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fail-case
  template:
    metadata:
      labels:
        app: fail-case
    spec:
      containers:
      - name: nginx
        image: nginx:latest
