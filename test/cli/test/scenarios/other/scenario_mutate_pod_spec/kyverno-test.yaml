name: scenario_mutate_pod_spec
policies:
   - test/policy/mutate/policy_mutate_pod_spec.yaml
resources:
   - test/resources/resource_mutate_pod_spec.yaml
results:
   - policy: mutate-pods-spec
     patchedResource: test/output/output_mutate_pod_spec.yaml
     resource: nginx-deployment
     kind: Deployment
     rule: disable-servicelink-and-token
     result: pass
