apiVersion: kyverno.io/v2
kind: ClusterCleanupPolicy
metadata:
  name: cluster-cleanup-namespace
spec:
  schedule: '*/1 * * * *'
  match:
    any:
      - resources:
          kinds:
            - Namespace
          name: cleanup-ns

