apiVersion: kyverno.io/v2
kind: CleanupPolicy
metadata:
  name: cleanup-pod-by-name
  namespace: test
spec:
  schedule: '*/1 * * * *'
  match:
    any:
      - resources:
          kinds:
            - Pod
          name: cleanup-pod-1

